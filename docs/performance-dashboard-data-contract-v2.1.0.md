<div dir="rtl" style="text-align: right;">

# Data Contract نهائي — الصفحة الرئيسية | لوحة الأداء EcomModa

<div style="background:#f0f9ff;border:2px solid #bae6fd;border-radius:8px;padding:8px 14px;display:inline-block;font-family:monospace;font-size:14px;direction:ltr;">
<strong>v2.1.0</strong>
</div>

آخر تحديث: 📅 2026-08-25 🕐 —

> **معتمد — جاهز للتنفيذ.** كل قاعدة هنا متحقَّق منها على أوردرات حيّة على
> API `2026-01`. الأوردر المرجعي لكل قاعدة مذكور في القسم 8.
>
> **v2 تلغي v1 بالكامل** — v1 كانت على مستوى الأوردر، دي على مستوى القطعة.
> **v2.1.0 تعديل على v2.0.0** — تحديث حدود صفحات مرحلة 2 بس (قسم 6)، **بدون
> أي تغيير في منطق التصنيف نفسه** (أقسام 1–5 زي ما هي حرفيًا).

> ℹ️ **ملحوظة تسمية:** المستند ده اتكتب وقت ما الأداة كان اسمها **"لوحة
> مبيعات" (Sales Dashboard)**. الأداة اتنقلت لـ git واتسمّت رسميًا
> **"لوحة الأداء" (Performance Dashboard)** — نفس الأداة، نفس المنطق، الاسم
> بس اللي اتغيّر. الملف ده هو الاستمرارية الرسمية لنفس الـ Data Contract،
> مش مستند جديد.

---

## 1. القرار اللي الشاشة بتدعمه

> **"كل جنيه دخل النظام راح فين؟"**

الصف الأول = **جسر بالقيمة**، وحدته **الجنيه** مش الأوردر. الأوردر الواحد ممكن
تتوزّع قيمته على أكتر من مربع — وده المقصود، مش خلل.

المعدلات بالعدد (نسبة RTO · معدل التسليم) → **صف تاني منفصل بصرياً**. ممنوع خلط
قيمة وعدد في نفس الصف.

---

## 2. المبدأ الحاكم

الحساب على مستوى **القطعة** مش الأوردر. لكل سطر:

```
Q = quantity                    (الكمية الأصلية)
C = currentQuantity             (الكمية الباقية)
P = discountedUnitPriceSet      (سعر القطعة)

راح  = (Q − C) × P
عاش  =  C × P
```

**هوية رياضية مضمونة:**
```
إجمالي القيمة = قيد التنفيذ + الفاقد + صافي المبيعات
```

---

## 3. المصادر الثلاثة

| السؤال | المصدر |
|---|---|
| القطعة نوعها إيه؟ (اتشالت · رجعت · بديلة) | **شوبيفاي** |
| الأوردر الأصلي اتسلّم؟ | **S1** = `Delivered` |
| دورة الإرجاع/الاستبدال خلصت فعلياً؟ | **S2** + `returns[].status` |

**السبب:** شوبيفاي بيعمل `Fulfilled` عند **الشحن** مش عند التسليم — فهو أعمى تماماً
بعد ما الأوردر يخرج من المخزن. S1/S2 هما المصدر الوحيد لما بعد الشحن.

---

## 4. قاعدة التصنيف الكاملة

### 4.1 الأوردر ملغي؟

```
cancelledAt ≠ null  +  displayFulfillmentStatus = UNFULFILLED  → كل القيمة → الفاقد ← ملغي
cancelledAt ≠ null  +  displayFulfillmentStatus = FULFILLED    → كل القيمة → الفاقد ← RTO
```

⚠️ ده يسبق أي فحص تاني. `manual_status` **مصدر ثانوي فقط** — ثبت إن Custom Flow كان
بيكتب `Cancelled` على أوردرات RTO.

### 4.2 تسوية دورات الإرجاع

```
دورة مُسوّاة  =  return.status = CLOSED
                  AND s2 ∈ { 'In-Return', 'Returned' }

CANCELED / DECLINED  →  تُتجاهل بالكامل
OPEN / REQUESTED     →  غير مُسوّاة — نتيجتها لا تُطبَّق
```

الشرطان مع بعض: `status` بيقول **أنهي دورة** (سجل دائم لكل دورة على حدة)، و S2 بيقول
**الوضع الفيزيائي** للدورة المفتوحة حالياً.

### 4.3 توزيع الجزء اللي راح — كمّي مش ثنائي

```
راح_الكلي     = Q − C
مرتجع_مُسوّى  = Σ returnLineItems.quantity  عبر الدورات المُسوّاة فقط
محذوف         = راح_الكلي − مرتجع_مُسوّى
```

لكل `returnLineItem` في دورة مُسوّاة:

| شرط الدورة | التصنيف |
|---|---|
| `exchangeLineItems` فاضي | الفاقد ← **مرتجع نهائي** |
| `exchangeLineItems` فيه سطور | الفاقد ← **مرتجع استبدال** |

الباقي (`محذوف`) → الفاقد ← **محذوف قبل الشحن**

⚠️ **السطر الواحد ممكن يكون في أكتر من دورة، وممكن يكون فيه تعديل ومرتجع مع بعض.**
الربط تراكمي بالكمية، مش واحد-لواحد.

### 4.4 توزيع الجزء اللي عاش

```
السطر بديل من دورة مُسوّاة    →  صافي المبيعات ← بديل
السطر بديل من دورة غير مُسوّاة →  قيد التنفيذ  ← بديل قيد التسليم
غير كده  →  s1 = 'Delivered' ?  صافي المبيعات  :  قيد التنفيذ
```

**السطر بديل** = `id` موجود في `returns[].exchangeLineItems[].lineItem.id`

---

## 5. المربعات

| # | المربع | المحتوى |
|---|---|---|
| 1 | **إجمالي القيمة** | `Σ (Q × P)` — الأساس 100% |
| 2 | **قيد التنفيذ** | تحت التأكيد · جاهز · مع المندوب · **بديل قيد التسليم** |
| 3 | **الفاقد** | ملغي · RTO · محذوف قبل الشحن · مرتجع نهائي · **مرتجع استبدال** |
| 4 | **صافي المبيعات** | مُسلَّم · **بديل** |

### قواعد عرض إلزامية

1. **"مرتجع استبدال" لا يُدمج مع "مرتجع نهائي".** الأول تبديل، التاني خسارة.
2. **مفيش أرقام سالبة.** نموذج القطعة ألغى الحاجة ليها.
3. **`subtotal` فقط.** الشحن مستبعد — تاب منفصل. `totalPriceSet` ممنوع (بيكبر برسوم
   الشحن المرتجع بعد التسليم).
4. **مؤشر حداثة** إلزامي مع عمر ملوّن.
5. **الاسم "إجمالي القيمة"** — مش "إجمالي المبيعات". الاستبدال بيضخّمه بقيمة البديل،
   والفرعي "مرتجع استبدال" بيفسّر المصدر.

---

## 6. جلب البيانات — مرحلتان

### المرحلة 1 — كل الأوردرات · `first: 250`

```graphql
orders(first: 250, query: "created_at:>=X created_at:<=Y", after: $cursor) {
  pageInfo { hasNextPage endCursor }
  nodes {
    id
    cancelledAt
    displayFulfillmentStatus
    s1: metafield(namespace: "custom", key: "manual_status") { value }
    s2: metafield(namespace: "custom", key: "status_2_r_e")  { value }
    lineItems(first: 25) {
      pageInfo { hasNextPage }
      nodes {
        id
        quantity
        currentQuantity
        discountedUnitPriceSet { shopMoney { amount } }
      }
    }
  }
}
```

`requestedQueryCost: 123` من سقف 2,000 · ~12 صفحة لـ 3,000 أوردر · ثانية بين الصفحات.

> ⚠️ **«ثانية بين الصفحات» منسوخة — 30-08-2026 (`index.js` v3.5.0).**
> `ecommoda-dashboard-builder` v2.1.0 Step 5 بيمنع أي `sleep` ثابت بين دفعات
> الجلب: بيخلي مدة الطلب دالة في **عدد الصفحات** مش في سرعة شوبيفاي (~٢٧ ثانية
> نوم مقصود لكل شهر هنا)، والانتظار المشروع الوحيد هو backoff الـ `THROTTLED`.
> نفس الشيء على `sleep(500)` بين دفعات المرحلة 2 تحت. **الثوابت المقفولة في §6
> (`LINE_ITEMS_PAGE` · `RETURNS_PAGE` · `RETURN_LINES_PAGE` ·
> `EXCHANGE_LINES_PAGE` · `STAGE1_PAGE_SIZE` · `STAGE2_BATCH_SIZE`) ما اتلمستش
> — السطر ده تقدير تكلفة، مش واحد منهم.**
> العلاج الهيكلي (التقسيم الشهري) اتنفّذ في نفس التسليم، فعدد الصفحات في النداء
> الواحد بقى محدود بشهر. → `CLAUDE.md` قرارات معتمدة 30-08-2026.

**حارس إلزامي:** لو `lineItems.pageInfo.hasNextPage = true` على أي أوردر → **الطلب
يفشل برسالة واضحة تحمل رقم الأوردر**. أوردر بأكتر من 25 صنف مستبعد بقرار العمل، والحارس
موجود عشان لو حصل يبان بدل ما الرقم ينقص بصمت.

### المرحلة 2 — المرشّحون فقط · دفعات 10

**الترشيح:** `Σ(Q) ≠ Σ(C)` **أو** `s2 ≠ null`

```graphql
nodes(ids: [...]) {
  ... on Order {
    id
    returns(first: 10) {
      pageInfo { hasNextPage }
      nodes {
        status
        returnLineItems(first: 25) {
          pageInfo { hasNextPage }
          nodes {
            quantity
            ... on ReturnLineItem { fulfillmentLineItem { lineItem { id } } }
          }
        }
        exchangeLineItems(first: 20) {
          pageInfo { hasNextPage }
          nodes { lineItem { id } }
        }
      }
    }
  }
}
```

> ### ⚠️ تعديل v2.1.0 (25-08-2026) — `returns` و `exchangeLineItems` اتضاعفوا
>
> ```
> returns(first: ...)          5  → 10
> exchangeLineItems(first: ...) 10 → 20
> returnLineItems(first: ...)  25 → 25   (زي ما هي — مفيش تغيير)
> ```
>
> **السبب:** الحارس الإلزامي (نفس مبدأ الحارس في مرحلة 1) كان بيوقف الطلب
> بالكامل — مش بيرجّع رقم ناقص — لما أوردر حقيقي يتخطى الحد القديم (أكتر من
> ٥ دورات إرجاع/استبدال، أو دورة فيها أكتر من ١٠ سطر بديل). ده حصل فعليًا على
> أوردرات حقيقية (مؤكَّد من أحمد 25-08-2026)، يعني الحد القديم مكنش سقف نظري —
> كان بيمنع تقارير فترات فيها الأوردرات دي من التحميل خالص.
> **التصحيح رفع الحد، مش إلغاء الحارس** — الحارس نفسه فضل زي ما هو (نفس مبدأ
> "صفحة فشلت = الطلب كله فشل" في `ecommoda-dashboard-builder`)، وده **إضافة
> سعة، مش تغيير في منطق التصنيف** — أقسام 1–5 فوق زي ما هي حرفيًا.
>
> **أثر على الكاش:** `CACHE_VERSION` في `index.js` لازم يترفع (`v6` → `v7`)
> معاه — تفاصيل التنفيذ في مستند التسليم لـ Claude Code.
>
> **مفتوح — يحتاج أرقام أوردرات فعلية لو الجدول في قسم 8 هيتحدّث بنفس الصرامة:**
> أحمد أكّد إن فيه أوردرات حقيقية وصلت للحد القديم ووقفت، لكن مفيش رقم أوردر
> محدد مسجّل هنا لحظة كتابة هذا التحديث. لو حابب توثيقها بنفس مستوى الدقة في
> قسم 8 (زي #49495 وغيره)، ابعتها وهتتضاف.
>
> **مفتوح — تكلفة الاستعلام:** `requestedQueryCost: 108` للأوردر (رقم v2.0.0
> الأصلي) كان مقاس على الحدود القديمة (`5`/`10`). الحد الجديد (`10`/`20`)
> غالبًا هيرفع التكلفة تقريبًا للضعف لكل أوردر فيه دورات إرجاع كتير — الرقم
> الدقيق محتاج قياس فعلي بعد النشر، مش افتراض.

`requestedQueryCost: 108` للأوردر (v2.0.0 — **يحتاج إعادة قياس بعد v2.1.0**، شوف
الصندوق فوق) → **10 أوردرات/دفعة**.

### الكاش

KV فقط · **الكتابة بعد نجاح آخر صفحة من المرحلتين** · مفيش Cron · التحديث بالزر
أو بتغيير الفترة (+ استثناء واحد موثّق تحت).

> ⚠️ **«فترة مقفولة → دائم» منسوخة — 30-08-2026 (`index.js` v3.2.0).**
> `ecommoda-dashboard-builder` v2.0.0 Step 3-أ/ج: قاعدة «الفترة المقفولة = كاش
> دائم» صح للأحداث بس، وغلط لصف بيوصف **حالة حالية**. `boxes`/`orderBoxes` هنا
> حالة أوردر حالية (`manual_status`/`status_2_r_e`/bucket)، وأوردر اتعمل في شهر
> مقفول ممكن يتحرك `Shipped → Delivered → Returned` بعد ما الشهر يتكاش — فالكاش
> الدائم كان بيثبّت الرقم على صورة قديمة للأبد من غير أي خطأ ظاهر.
> **الساري:** `ttlFor(dateTo)` متدرّج وعمره ما يرجّع `null` —
> نطاق فيه النهارده `900` · قفل من ٤٥ يوم أو أقل `21600` · أقدم من كده `86400`.

**شبكة الكاش — هجينة** (إلزامي يتكتب هنا: `ecommoda-dashboard-builder`
Step 3-د-١ · checklist «شبكة الكاش المختارة مكتوبة في الـ Data Contract»):

| الحالة | المفتاح اللي بيتقرا |
|---|---|
| مفتاح الشهر التقويمي موجود | اقرا منه وفلتر الأطراف في الواجهة |
| مش موجود · الفترة ≤ ٣١ يوم · مش على حدود شهر | اسحب الفترة نفسها بمفتاحها هي |
| غير كده (فترة واسعة أو مطابقة لحدود شهور) | شبكة شهور تقويمية كاملة، شهر ورا شهر |

> ⛔ **ممنوع قصّ الـ chunk على حدود اختيار المستخدم.** مفتاح الكاش هو **نصّ
> التاريخين حرفيًا**، فأي قصّ بيولّد مفتاح يتيم لكل فترة مخصصة — و"آخر ٧ أيام"
> بتولّد واحد **كل يوم**. الأطراف بتتفلتر في الواجهة بـ `createdAt` بس.

> 🔴 **الفلترة دي بتوقيت المتجر (`Africa/Cairo`)، مش UTC** — `index.html`
> v3.7.0. شوبيفاي بتفسّر `created_at:` بتوقيت المتجر: فلتر يوم `2026-08-25`
> رجّع أوردرات من `2026-08-24T21:06Z` لحد `2026-08-25T20:59Z` (قياس مباشر
> 01-09-2026). فمقارنة `createdAt.slice(0,10)` (تاريخ UTC) بحدود الفترة
> (تواريخ قاهرة) كانت بتنقل كل أوردر اتعمل بين ١٢ و٣ الفجر لليوم اللي قبله.

**الاستثناء الوحيد من «التحديث بالزر أو بتغيير الفترة»** — الضغط على **مربّع
الشهر** بيبعت `forceRefresh: true` للشهر ده (الضغطة *هي* الزرار)، ومعاه **حارس
حداثة** `MONTH_BOX_FRESH_MINS = 5`: كاش أطزج من كده بيتقرا بدل دورة GraphQL
كاملة. المربّع بياخد **الشهر التقويمي كامل** حتى للشهر الحالي (مش "لحد
النهارده" — ده كان هيولّد مفتاح جديد كل يوم ويلغي سبب وجود المربّع أصلاً).

**البوابة بالتكلفة مش بوجود نقص** (Step 3-د-٢): `GATE_MIN_MISSING_MONTHS = 1` —
شهر واحد ناقص بيتحمّل تلقائيًا بشريط تقدّم، والبوابة من شهرين فما فوق. الـ chunk
الـ `exact` (المسار القصير) **مستثنى من العدّ** — الـ miss فيه هو الوضع الطبيعي.

### الحد الأدنى للبيانات — `STORE_START_DATE = 2026-05-01`

قرار أحمد 31-08-2026: اللي قبل بداية تشغيل المتجر يتعامل معاه **كأن المتجر
ماكانش شغّال** — مفيش سحب، مفيش مفتاح كاش، ومفيش رقم على الشاشة. الثابت في
**`index.js` و`index.html` مع بعض**، والطبقتين مختلفتين في الوظيفة عن قصد:
الـ Worker **بيرفض** بـ 400 (`validateDateRange` — choke point واحد بيغطي
`get_data` و`get_meta`)، والواجهة **بتقصّ قبل ما تسأل** (`clampRangeToStoreStart`).

> ⛔ **الـ Worker بيرفض ومبيقصّش:** القصّ الصامت يا بيكتب نفس المحتوى تحت
> مفتاحين، يا بيرد على فترة **غير** اللي الواجهة سألت عنها — فالـ probe بتاعها
> يفضل يقول "مش متكاش" للأبد. `get_config` بيرجّع القيمة والواجهة بتقارنها.

> ⚠️ **المقارنة (Δ%) بتتلغي، مبتتقصّش** — `getPreviousRange` بترجّع `null` لو
> الفترة السابقة مش داخلة **كلها** بعد الحد. مقارنة ٩٠ يوم بـ ٣٣ يوم مقصوصة =
> رقم غلط شكله سليم، أخطر من غياب المقارنة.

### الوصول

`WORKER_SECRET` على **كل** الطلبات (بما فيها الـ auth) · CORS wildcard —
الأداة قراءة فقط ومفيش راوت واحد بره البوابة.

> ⚠️ **«بدون تسجيل دخول» منسوخة — 23-08-2026 (`index.js` v3.0.0).**
> اتضاف تسجيل دخول D1 القياسي (موظف + PIN): `check_employee` · `register_pin` ·
> `verify_employee` · `log_logout` · `get_employees`، وبيكتب في `logs` بـ
> `tool = 'performance_dashboard'` و`type ∈ {login, logout}` — **دول بس، مفيش
> type تالت** (مسجَّل في `ecommoda-constants` §7). طبقة **فوق** الـ
> `WORKER_SECRET`، مش بديل عنه. والبيانات بتتحمّل **بعد** الدخول مش قبله.

---

## 7. الحقول المستبعدة

| الحقل | السبب |
|---|---|
| `subtotalPriceSet` · `currentSubtotalPriceSet` | تُحسب من السطور — تُستخدم للتحقق أثناء التطوير فقط |
| `totalPriceSet` · `currentTotalPriceSet` | ملوّثة برسوم الشحن المرتجع |
| `displayFinancialStatus` | لا يدخل في أي حساب |
| `customer` · `shippingAddress` | تابات تانية — Data Contract مستقل |

> ⚠️ **`name` و`createdAt` خرجوا من قائمة المستبعدين** — بيتجابوا فعليًا من
> المرحلة 1 وبقوا **حاملين وظيفة**، مش تجميل:
> `name` = رقم الأوردر في جدول التفاصيل + الهايبرلينك لشوبيفاي (v1.0.1)،
> و`createdAt` = **أساس فلترة أطراف الفترة** بعد التقسيم الشهري (`visibleRows`،
> v3.5.0) — من غيره الـ chunk الشهري مايتقصّش على اختيار المستخدم أصلاً.
> أي حذف ليهم = كل رقم على الشاشة بيوصف الشهر بدل الفترة المعروضة.

---

## 8. الأدلة

| القاعدة | الدليل |
|---|---|
| `subtotal = Σ (Q × P)` · `currentSubtotal = Σ (C × P)` | #49495 (21,100 / 11,900) — و 7 أوردرات قبلها |
| `subtotal` بيحتفظ بالأصلي بعد الإلغاء | #49406 · #49472 |
| RTO = `cancelledAt` + `FULFILLED` | #49472 (والميتافيلد كان بيقول `Cancelled` غلط) |
| `returnLineItems → lineItem.id` ربط سليم | #49486 · #49495 |
| `exchangeLineItems` فاضي/مليان = إرجاع/استبدال | #49474 · #49486 |
| الإرجاع الجزئي بالكمية داخل السطر | #49495 (Black: Q=4 → C=2) |
| تعديل ومرتجع على نفس السطر | #49495 (Skychaser محذوف · Black مرتجع) |
| السطر الواحد في أكتر من دورة | #49495 (Black في R1 و R2) |
| دورات متعددة بـ `status` مستقل | #49486 (3 قياسات) |
| `first: 250` = 123 نقطة | فترة 2026-07-01+ |
| **(v2.1.0)** `returns(first: 5)`/`exchangeLineItems(first: 10)` بيوقفوا طلبات حقيقية | مؤكَّد من أحمد 25-08-2026 — **رقم أوردر مرجعي لسه ناقص** |

---

## 9. تصحيحات إلزامية على المهارات

**تُطبَّق قبل أي محادثة بناء جديدة، وإلا هتشتغل بمعلومات قديمة.**

| المهارة | التصحيح |
|---|---|
| `ecommoda-order-lifecycle` | RTO يُحدَّد من `cancelledAt` + `displayFulfillmentStatus`، والميتافيلد ثانوي |
| `ecommoda-order-lifecycle` | ثغرة `s2_type` **محلولة من الـ API** عبر `exchangeLineItems` — لا حاجة لميتافيلد جديد |
| `ecommoda-order-lifecycle` | `classifyOrder()` لا تصلح للتقارير المالية — الحساب على مستوى القطعة |
| `shopify-graphql-helper` | `exchangeLineItems` **connection** ويقبل `first:` و `nodes` (المدوَّن حالياً غلط) |
| `shopify-graphql-helper` | `totalPriceSet` **ليس** مجمّداً — بيكبر برسوم الشحن المرتجع بعد التسليم |

---

## 10. مفتوح

1. **اختبار الخصم** — هل `discountedUnitPriceSet` بيشمل خصم الكوبون على مستوى الأوردر؟
   يؤثر على **قيمة** المربعات، **لا يؤثر على بنية النموذج**. غير معطّل للتنفيذ.
2. **"غير مستقر"** — الدورات المفتوحة مستبعدة من الحساب. عرض عددها كمؤشر ثانوي:
   قرار واجهة مؤجَّل.
3. **الصف الثاني** (المعدلات بالعدد) — Data Contract مستقل.
4. **(v2.1.0)** رقم الأوردر المرجعي لتعديل حدود مرحلة 2 (قسم 8) — لو أحمد قدر يوفره.
5. **(v2.1.0)** إعادة قياس `requestedQueryCost` الفعلي لمرحلة 2 بعد الحد الجديد.

---

آخر تحديث: 01-09-2026 — **تصحيحات توثيقية بس، مفيش Data Contract جديد.**
أقسام 1–5 (منطق التصنيف) والثوابت المقفولة في §6 **ما اتلمستش حرفيًا**؛ اللي
اتصلّح هنا هو أوصاف بقت مخالفة للكود المنشور (الكاش · الوصول · §7) وبنود كان
`ecommoda-dashboard-builder` بيفرض كتابتها هنا ومكانتش مكتوبة (شبكة الكاش
الهجينة · فورس مربّع الشهر · البوابة · `STORE_START_DATE`).
النسخة الأصلية: 25-08-2026 (v2.1.0 — تعديل على v2.0.0 بتاريخ 02-08-2026)

</div>
