<div dir="rtl" style="text-align: right;">

# لوحة الأداء (`Performance-Dashboard`)

![version](https://img.shields.io/badge/version-v1.8.0-blue)

**بتعمل إيه:** داشبورد قراءة فقط بيجمّع بيانات أوردرات شوبيفاي على مستويين مستقلين —
تقييم بالقطعة (فلوس) وعدّ الأوردرات — لفترة زمنية محددة، مع كاش على KV. وتاب تالت
(**المحافظات والمناطق**) بيقسّم نفس الأرقام جغرافيًا على محافظة الشحن وعلى مناطق
التوزيع — بتقسيمة تلاتية موحّدة (تسليمات · إلغاءات ومرتجعات · قيد التنفيذ) بالعدد
وبالقيمة مع بعض.
**مين بيستخدمها:** إدارة · حسابات
**الإصدار:** Worker `v3.7.0` · الواجهة `v3.11.0` ← الاتنين مستقلين، طبيعي يختلفوا
(اتصادفوا على نفس الرقم في تعديلين سابقين بس — مش قاعدة، وافترقوا تاني في 01-09-2026)

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | v2.0.0 |
| ecommoda-html-builder | v6.0.0 |
| ecommoda-constants | v1.4.3 |
| ecommoda-dashboard-builder | v3.1.0 |
| ecommoda-order-lifecycle | v1.2.0 |

آخر مطابقة: 02-09-2026 · `index.js` v3.7.0 · `index.html` v3.11.0
🔴 معلّقة: **خطوة (ج) من G-13** — `orderCycleRows[]` (مصفوفة صف لكل دورة جنب
المربع، `classification-rules.md` §9-ب). مؤجّلة بقرار. من غيرها المربع الواحد
بيفضل بيوصف **أحدث دورة بس**، والدورة الأقدم المكتملة مش ظاهرة في أي KPI —
وده اللي وسم `MULTI_CYCLE` بيعلن عنه صراحةً بدل ما يفضل صامت.

> `v1.0.0` = **قبل النظام**، مش شهادة مطابقة. → `ecommoda-skill-versioning`

## الروابط

```
الواجهة    : https://ecommoda-dev.github.io/Performance-Dashboard/
الـ Worker : https://performance-dashboard-worker.ecommoda-dev.workers.dev
اسم الـ Worker في الداشبورد: performance-dashboard-worker   ← لازم يطابق name في wrangler.toml
```

> ⚠️ **اتغيّر 01-09-2026:** عنوان الـ Worker بقى **constant في الواجهة**
> (`WORKER_URL` في `§CONFIG`) — مش حقل إعدادات ومش `localStorage`
> (`ecommoda-html-builder` v2.2.0 · Standards Changelog #28). `WORKER_SECRET`
> وحده هو اللي فضل حقل إعدادات + `localStorage` (`perfdash_worker_secret`)،
> وهو **مفتاح الـ localStorage الوحيد** في الأداة دلوقتي.
> (`perfdash_worker_url` و`admin_worker_url` بيتمسحوا مرة واحدة عند التحميل.)

## الـ Endpoints

> **كل** الطلبات (بما فيها الـ auth) ورا بوابة `Authorization: Bearer ${WORKER_SECRET}`.
> مفيش راوت واحد بره البوابة.

| `?action=` | Method | بيعمل إيه |
|---|---|---|
| `check_employee` | GET | بيشوف الموظف مسجّل PIN ولا لأ |
| `register_pin` | POST | تسجيل PIN أول مرة |
| `verify_employee` | POST | تسجيل الدخول (+ `writeLog` type `login`) |
| `log_logout` | GET | تسجيل الخروج (+ `writeLog` type `logout`) |
| `get_employees` | GET | قائمة الموظفين النشطين للـ dropdown |
| `get_data` | GET/POST | ⚠️ (31-08-2026) `get_data` و`get_meta` بيرفضوا بـ 400 أي فترة بتبدأ قبل `STORE_START_DATE` (`2026-05-01`). البيانات المجمّعة — `boxes`/`rows`/`warnings` + `orderBoxes`/`orderRows`/`orderWarnings`. كل صف في `rows` و`orderRows` فيه كمان `prov` (محافظة الشحن) و`zone` (`custom.zone`) — تاب المحافظات بيتحسب منهم (v3.7.0). محتاج `dateFrom` و `dateTo`. الواجهة بتناديه **مرة لكل شهر بالتسلسل** (30-08-2026) وبتحسب كل الأرقام من `rows`/`orderRows` — الملخّصات (`boxes`/`orderBoxes`) بتوصف الـ chunk كامل فمش بتتعرض مباشرة |
| `get_meta` | GET | ميتاداتا الكاش لفترة (`lastUpdated` + `count`). محتاج `dateFrom` و `dateTo`. **وظيفتان:** عرض عمر البيانات، و**probe** بيقول أنهي شهور متكاشة قبل التحميل — الاتنين قراءة KV بحتة، ممنوع يلمس شوبيفاي |
| `clear_cache` | GET | يمسح كاش فترة واحدة، أو الكاش كله لو الفترة مش متبعتة |
| `diag` | GET | فحص ذاتي بدون كتابة — متغيّرات/bindings/صلاحيات شوبيفاي/D1/KV (30-08-2026) + **قراءة `shippingAddress.province` و`custom.zone` على أوردر حقيقي** (01-09-2026 · v3.7.0) — الحقل الوحيد اللي بيرجع `null` بصمت لو صلاحية بيانات العملاء المحمية ناقصة |
| `get_config` | GET | يرجّع `WORKER_VERSION` + `CACHE_VERSION` + `storeStartDate` — للمقارنة مع نسخة الواجهة وحدّها (30-08-2026 · 31-08-2026) |

> **مفيش `get_logs` في الأداة دي** — بتكتب في D1 بس، مبتقراش السجل.

## D1

```
tool  : performance_dashboard
type  : login · logout          ← دول بس، مفيش أي type تاني في الكود
```

الجداول: `logs` (كتابة) · `employees` (قراءة + تحديث `last_login`/`pin`) — الجدولين
المشتركين، **مفيش أي جدول خاص بالأداة دي**.

## المضبوط فعليًا في الداشبورد

```
Bindings : DB       → ecommoda-dev-logs                       (من wrangler.toml)
           DASH_KV  → d38bb41fa5a74f7b9ce116b23f5446e7        (من wrangler.toml)
Secrets  : WORKER_SECRET · CLIENT_ID · CLIENT_SECRET     ← في الداشبورد، مش في الريبو
Vars     : SHOP_DOMAIN · LOCATION_ID                     ← من [vars] في wrangler.toml
Build watch paths : * (الافتراضي) — لسه متضيّقتش
```

> `LOCATION_ID` **مش مستخدم في كود الـ Worker** (مفيش `env.LOCATION_ID` ولا مرة).
> متسيب كـ var قياسي، بس مش شرط لتشغيل الأداة.

## CORS

`wildcard *` — الأداة قراءة فقط (`get_data`/`get_meta`/`clear_cache`) + auth endpoints،
وكلها ورا `WORKER_SECRET`. الهيدرز في `CORS_HEADERS` والـ preflight أول حاجة في `fetch`.

## الثوابت المقفولة (من Data Contract v2.1.0 §6 — ممنوع تتغير من غير Data Contract جديد)

```
LINE_ITEMS_PAGE 25 · RETURNS_PAGE 10 · RETURN_LINES_PAGE 25
EXCHANGE_LINES_PAGE 20 · STAGE1_PAGE_SIZE 250 · STAGE2_BATCH_SIZE 10
CACHE_VERSION v12 · MAX_CACHE_BYTES 24MB
كاش: TTL متدرّج (ttlFor) — نطاق فيه النهارده = 900 ثانية · قفل من ٤٥ يوم أو أقل
     = 21600 ثانية (6 ساعات) · أقدم من ٤٥ يوم = 86400 ثانية (24 ساعة). عمره ما
     يبقى دائم (كان كذلك قبل 30-08-2026 — شوف قرارات معتمدة تحت).
مفاتيح KV: dash:performance_dashboard:v12:data|meta:<from>:<to>
           ⚠️ المفتاح دايمًا **شهر تقويمي كامل** لما الفترة تعدّي حدود شهر
           (2026-08-01:2026-08-31)، عشان "أغسطس" و"آخر ٣٠ يوم" و"آخر ٧ أيام"
           يقروا نفس المفتاح. فترة قصيرة شهرها مش متكاش بتاخد مفتاحها هي.
```

> **تحديث 25-08-2026:** RETURNS_PAGE و EXCHANGE_LINES_PAGE اتضاعفوا (5→10،
> 10→20) — كان فيه أوردرات حقيقية بتوقف الطلب كله بسبب الحد القديم. القرار
> من أحمد. CACHE_VERSION اترفع لـ v7 بالتبعية (قاعدة ecommoda-dashboard-builder).
> التفاصيل والدليل → `docs/performance-dashboard-data-contract-v2.1.0.md` §6.

## قرارات معتمدة

- **🔴 فلتر المحافظات + ليبلات أفقية + إلغاء التمرير التلقائي — 02-09-2026
  (`index.html` v3.11.0).** تلات طلبات من أحمد على التاب بعد أول عرض. التنفيذ
  **واجهة بحتة** برضه: صفر تعديل في `index.js`، صفر `CACHE_VERSION` bump.

  - **🔴 فلتر المحافظات (chips) بيقصّ التاب كله.** `state.geoProvSel` — **Set
    فاضية = مفيش فلتر = كل المحافظات** (نمط الـ facets القياسي). البديل (كله
    مختار في الأول وبتشيل منه) كان معناه ٢٨ ضغطة عشان تشوف محافظة واحدة.
  - **🔴 الفلترة على الصفوف الخام، قبل أي تجميع** (`geoFilterRows` جوّه
    `computeGeoGroups`). ⛔ **ممنوع** الفلترة على المجموعات بعد ما تتحسب:
    قسم المناطق بيتجمّع على `zone` مش على `prov`، فكان هيفضل بيعدّ **كل**
    الأوردرات والصفحة تعرض رقمين بمقاسين مختلفين جنب بعض من غير أي إشارة.
    ونفس السبب خلّى `geoSelectedRows()` (جدول الأوردرات تحت) تتفلتر كمان.
  - **قايمة الـ chips بتتبني من الصفوف غير المفلترة** — لو اتبنت من المفلترة،
    أول اختيار كان هيمسح باقي الـ chips من الشاشة ويقفل الطريق على تعديله.
  - **الـ chips بتعرض كل محافظات شوبيفاي، والفاضية معطّلة مش مخفية.** اختيار
    محافظة بلا أوردرات = صفحة فاضية، وإخفاؤها = قايمة بتتغيّر شكلها مع كل
    فترة. الحل: باهتة ومش قابلة للضغط.
  - **«الكل» بترجّع لـ Set فاضية، مبتملهاش بكل المفاتيح.** Set مليانة كانت
    هتقفل الاختيار على المحافظات الموجودة **في الفترة دي**، فتغيير الفترة
    بعدها كان هيخفي محافظة جديدة من غير ما حد ياخد باله.
  - **شارة «🔎 مفلتر على N محافظة» على كل عنوان قسم** (`geoFilterFlag`) —
    من غيرها الرقم في أي جدول بيتقري كإجمالي الفترة وهو إجمالي الاختيار.
    نفس عيلة «رقم غلط شكله سليم».
  - **⚠️ المجموعة المفتوحة تحت بتتقفل لو اتشالت من الاختيار** (`applyGeoFilter`)
    — سيبانها مفتوحة معناه جدول أوردرات بعنوان محافظة مش ظاهرة في أي رقم فوقه.
  - **🔴 ممنوع ليبلات مايلة في أي bar chart — قرار أحمد.** `maxRotation: 0`
    على رسوم التاب دي **وعلى رسمي تاب المبيعات وتاب الأوردرات**. الأثر:
    عرض المجموعة اتحسب من **أعرض ليبل** (`GEO_GROUP_W` = ٧٠px للمحافظة ·
    ١٢٠px للمنطقة — أعرض اسم مقيس ٦١px «مرسى مطروح» و٨٩px «قاهرة وجيزة
    (مناديب)»)، فالعرض الافتراضي بقى ٢١٧٠px وبيتمرّر أفقيًا على شاشة ١٣٦٢px.
    ⚠️ ده **تمن مقبول ومقصود**، والمخرج منه هو الفلتر نفسه. ⛔ ممنوع ترجيع
    `maxRotation` كـ«حل» لضيق العرض.
  - **`maxBarThickness` من ٢٦ لـ ٥٤.** السقف بيبقى مُلزِم **بس** لما المجموعات
    قليلة (بعد الفلترة) — ساعتها الخانة واسعة والعمود بـ٢٦px كان بيبان خيط
    تايه في نص فراغ. مع كل المحافظات العرض الطبيعي ≈١٧px فالسقف ما بيتفعّلش.
  - **🔴 `scrollIntoView` اتشال من `showGeoDetails` — قرار أحمد.** الضغطة على
    صف أو عمود هي فعل **فلترة**، والقفزة لجدول تحت الشاشة بتوديك بعيد عن
    الحاجة اللي لسه بتقارن فيها وبتخليك ترجع بإيدك كل مرة. نفس القرار اللي
    اتاخد قبل كده على مربعات تاب المبيعات وتاب الأوردرات — التاب الجديد كان
    آخر مكان فاضل فيه السلوك ده.

- **🔴 إعادة تصميم تاب «المحافظات والمناطق» — 02-09-2026 (`index.html` v3.10.0).**
  طلب أحمد بالحرف، والتنفيذ **واجهة بحتة**: صفر تعديل في `index.js`، صفر
  `CACHE_VERSION` bump، صفر نداء إضافي. كل رقم جديد على الشاشة مشتق من نفس
  `state.rows`/`state.orderRows` بنفس دالتي التابين التانيين.

  - **التاب اتسمّى «المحافظات والمناطق».** الاسم القديم كان بيوصف نص التاب بس —
    قسم مناطق التوزيع جوّاه من أول يوم وبنفس الحجم. الزرار هو **مصدر الاسم
    الوحيد** (`TAB_DEFS` + `updateTabChip` بيقروا `textContent`)، فالتغيير في
    مكان واحد بيتنقل للـ chip بتاع الموبايل لوحده.
  - **🔴 التقسيمة بقت تلاتية موحّدة: تسليمات · إلغاءات ومرتجعات · قيد التنفيذ**
    (`GEO_STATUS` — مصدر واحد للاسم واللون، بيستهلكه الرسمين والجدولين
    والمفتاح). الأعمدة القديمة (صافي المبيعات · إجمالي القيمة · الفاقد ·
    إلغاءات لوحدها · خارج التصنيف) اتلمّت جوّه التلاتة.
  - **🔴 «قيد التنفيذ» بقى بيشمل `UNCLASSIFIED` — قرار أحمد صريح، وده عكس قرار
    01-09-2026.** التعريف اللي اتطلب حرفيًا: «كل حاجة **ليست نهائية**». القرار
    القديم (عمود مستقل) كان صح لغرضه، بس التقسيمة التلاتية بتلغي مكان تالت
    يقعد فيه. **العدد ما اتخفاش:** بيتعرض في تفسير عمود «قيد التنفيذ»
    (`HINT_PROG`)، وتحذير الصفحة (`orderWarnings.unclassified`) لسه بيولّع زي
    ما هو — فقاعدة `dashboard-builder` Rule 10 (الصف يقول *ليه*) لسه محفوظة.
    ⚠️ **جنب ده الفلوس كانت أصلاً كده**: `computeBoxesFromRows` بتحط
    `ipUnclassified` جوّه `inProgress` من الأصل، فالوحدتين بقوا متسقين بدل ما
    عمود بالعدد ينفصل وعمود بالقيمة يتبلع.
  - **🔴 «قيد التنفيذ» بيتحسب بالطرح مش بالجمع** — `الإجمالي − تسليمات −
    (إلغاءات ومرتجعات)` في الوحدتين (`geoRow`). السبب مش تجميلي: أي bucket جديد
    يتضاف في الـ Worker بيدخل «قيد التنفيذ» **تلقائيًا** بدل ما يختفي من التلات
    أعمدة بصمت ويكسر هوية «المجموع = الإجمالي». الهوية دي بقت مضمونة رياضيًا
    مش متأكَّدة بالفحص.
  - **🔴 الدونات اتشالت.** كانت بتجاوب على نفس سؤال عمود «الإجمالي» في الجدول
    («مين الأكبر؟») وبتخبّي ٢٠ محافظة في شريحة «باقي المحافظات» — يعني تلتين
    المحافظات بلا أي تمثيل بصري. البديل: **رسمان متراصّان بعرض الصفحة** لكل
    بُعد (`renderGeoCharts`) — واحد بالعدد وواحد بالقيمة، تلات أعمدة جنب بعض
    لكل مجموعة، **وبنفس ترتيب المجموعات بالظبط في الاتنين** (الرسم التاني
    بيتقري كـ«نفس الصف بالوحدة التانية»؛ ترتيب مستقل لكل رسم كان هيخلي
    المقارنة الرأسية كذب).
  - **الرسم بيعرض كل محافظات شوبيفاي حتى الفاضية** (`geoAllRows` +
    `geoEmptyRow`) — طلب أحمد. محافظة بصفر أوردرات **إشارة**، وإخفاؤها بيخلي
    الرسم يوصف عيّنة مش قايمة.
  - **⛔ مفيش `makeBarValueLabelsPlugin` على رسوم التاب ده.** ٢٩ محافظة × ٣
    أعمدة = ٨٧ رقم فوق الأعمدة بيتراكبوا ويبقوا لطخة. المحور الرأسي ظاهر
    بدالهم بلاحقة مختصرة (`geoAxisTick`)، والرقم الكامل + النسبة في الـ tooltip.
    ⚠️ **لاحقة المحور لاتينية (`K`/`M`) عن قصد** — التيك بيتكتب بـ `fillText`
    جوّه كانفس، وخلط رقم لاتيني بلاحقة عربية بيقلب ترتيبهم حسب سياق الـ bidi
    (طلعت «الف500» في القياس).
  - **⚠️ `direction: ltr` على حاوية سكرول الرسم (`.geo-chart-scroll`).** الصفحة
    RTL، فحاوية RTL بتبدأ مسكرولة على أقصى **اليمين** — والرسم بيرسم أكبر
    محافظة والمحور الرأسي على الشمال. النتيجة كانت أول مشهد = ذيل المحافظات
    الفاضية والمحور مقصوص برّه الشاشة (اتقاس بلقطة فعلية). الليبلات جوّه الرسم
    عربية وبتترسم صح في الحالتين.
  - **الجدول بقى ٤ أعمدة بالعدد + ٤ بالقيمة** (`إجمالي · تسليمات · إلغاءات
    ومرتجعات · قيد التنفيذ` × ٢) + عمود **«النسبة»** (كان اسمه «الحصة» —
    **الاسم بس هو اللي اتغيّر، التعريف زي ما هو**: نصيب الصف من إجمالي
    «تسليمات» بالقيمة). الـ `thead` بقى **سطرين** برأس مجموعة لكل تلتة، لأن
    «تسليمات» بقى اسم عمود متكرر مرتين بمعنيين مختلفين.
  - **الكروت الأربعة اتشالت** (محافظات فيها أوردرات · أعلى محافظة · تركّز أعلى ٣
    · أعلى معدل مرتجع + إلغاء) — قرار أحمد. معاهم راحت `GEO_MIN_SAMPLE`
    و`GEO_TOP_N` و`GEO_PALETTE` و`.kpi-card-static`/`.geo-value-text`/
    `.kpi-grid-4`، لأن مالهمش أي مستهلك تاني.
    ⚠️ الحد الأدنى للعيّنة كان موجود عشان **الكارت الترشيحي** بس — الجدول عمره
    ما استبعد صف، فما ضاعش أي حارس بشيله.
  - **🐛 إصلاح جانبي: ألوان الجدول ما كانتش بتتطبّق أصلاً.** `clr-red`/`clr-green`
    كانوا مكتوبين على خلايا جدول المحافظات من v3.9.0، لكن **كل** قواعد الألوان
    في الملف مقيّدة بـ `.kpi-value` / `.kpi-breakdown-value` — فالخلايا كانت
    بتطلع بلون النص العادي والكود شكله سليم. اتضافت قواعد مقيّدة بـ `.geo-table`
    للتلات ألوان.
  - **⚠️ صفر `CACHE_VERSION` bump** (فضلت `v12`) و**صفر تعديل في `index.js`**:
    لا شكل الاستعلام ولا شكل الصف ولا أي منطق تصنيف اتلمس — التغيير كله عرض.
    ⛔ وعشان كده **`MIN_WORKER_VERSION` ما اتحركتش** (فضلت `3.7.0`): الواجهة
    ما اعتمدتش على أي endpoint أو حقل جديد. القاعدة بترفع الرقم لما تعتمد على
    حاجة جديدة — مش مع كل تسليم.

- **🆕 تاب «المحافظات والمناطق» — 01-09-2026 (`index.js` v3.7.0 ·
  `index.html` v3.9.0).** طلب أحمد: تحليل أداء كل محافظة (مبيعات · عدد · نسبة
  التسليمات مقابل المرتجعات والإلغاءات)، وجنبه نفس التحليل على مناطق التوزيع من
  ميتافيلد `custom.zone`.

  - **🔴 المحافظة = `shippingAddress.province`، مش `city`.** ده كان صريح في
    الطلب وهو الصح: `city` حقل **حر بيكتبه العميل** — قياس مباشر على أوردرات
    القاهرة رجّع «القاهرة» و«6اكتوبر» و«فيصل» و«الشروق» كقيم مستقلة، فالتجميع
    عليها بيولّد عشرات المجموعات لنفس المحافظة. `province` قايمة مقفولة من
    شوبيفاي (٢٧ محافظة + الملغيتين: حلوان و٦ أكتوبر).
  - **🔴 مفتاح التجميع هو الاسم الإنجليزي، مش `provinceCode`.** الاتنين
    بيتجابوا، والاختيار مقصود: `PROVINCE_AR` مبنية على الأسماء، فاسم مش موجود
    فيها **بيتعرض خام** (النقص بيبان ويتصلّح). خريطة أكواد فيها كود غلط بتعرض
    **اسم محافظة تانية** — رقم غلط شكله سليم، وهو بالظبط اللي الأداة دي كلها
    متبنية عشان تتجنبه. الفولباك: الاسم → الكود → `null` («غير محدد»).
  - **🔴 `prov`/`zone` على **الصف**، مش أرقام مجمّعة في الـ payload.** نفس سبب
    `hasRE` في v3.5.0 حرفيًا: بعد التقسيم الشهري الـ payload بيوصف **شهر
    تقويمي كامل** والمعروض هو اختيار المستخدم بعد فلترة الأطراف بـ `createdAt`.
    أي تجميع جاهز في الـ Worker بيبقى رقم غلط شكله سليم على أي فترة مش مطابقة
    لحدود شهر. → `CACHE_VERSION` **v11 → v12** (شكل الصف اتغيّر).
  - **🔴 الفلوس والعدّ بيتحسبوا بنفس دالتي التابين التانيين** —
    `computeBoxesFromRows` و`computeOrderBoxesFromRows` على مجموعة جزئية، **مش**
    بتعريف جديد في `§GEO-JS`. تعريف تاني كان معناه إن أول تعديل في التصنيف
    يخلي «صافي المبيعات» في تاب المبيعات ≠ مجموع نفس العمود في تاب المحافظات،
    والفرق مايبانش. متأكَّد باختبار هوية: مجموع المجموعات = الإجمالي في
    الأربع أعمدة (عدد · صافي · إجمالي القيمة · فاقد)، على البُعدين.
  - **المقام = إجمالي أوردرات الصف — قرار أحمد.** النسب الخمسة (تسليمات ·
    مرتجعات · إلغاءات · قيد التنفيذ · خارج التصنيف) بتجمع **100%** دايمًا.
    ⚠️ الأثر المعروف والمقبول: على فترة لسه مفتوحة كل النسب بتطلع منخفضة لأن
    «قيد التنفيذ» بياخد نصيبه — وعشان كده العمود ده **معروض جنبهم** بدل ما
    يكون مقام مخفي، ومعدل التسليم بيتقري في سياقه.
  - ~~**`UNCLASSIFIED` عمود مستقل، مش متبلّع في «قيد التنفيذ».**~~ — **اتعكس
    02-09-2026 بقرار أحمد** (التقسيمة التلاتية، فوق). الغرض الأصلي محفوظ من
    طريق تاني: العدد بيتعرض في تفسير عمود «قيد التنفيذ» وتحذير الصفحة لسه
    بيولّع، فـ Rule 10 لسه مطبّقة — اللي اتغيّر هو **مكان** الإشارة مش وجودها.
  - ~~**`GEO_MIN_SAMPLE = 10` على كارت «أعلى معدل مرتجع + إلغاء» بس.**~~ —
    **اتشال 02-09-2026** مع الكروت الأربعة (قرار أحمد، فوق). الحارس كان موجود
    عشان الكارت الترشيحي لوحده والجدول عمره ما استبعد صف، فمشيه ما ضيّعش أي
    حماية. ⚠️ لو اترجع أي كارت ترشيحي بمعدل في المستقبل، **الحد الأدنى للعيّنة
    لازم يرجع معاه** — نفس علة `MIN_DELTA_BASE`.
  - **`zone`: `null` بيتجمّع مع `BLANK`، والقيمة الغريبة بتتعرض خام.** الميتافيلد
    الغايب و`BLANK` معناهم واحد عمليًا («المنطقة ما اتحدّدتش»). لكن تعريف
    الميتافيلد على شوبيفاي نفسه بيحذّر من **قيم غير صالحة** موجودة فعلًا على
    المتجر — فأي قيمة برّه الأربعة بتتعرض **خام بلون أحمر** بدل ما تتبلع في دلو
    «أخرى» يخفيها للأبد.
  - **⚠️ `shippingAddress` هو الحقل الوحيد اللي ممكن يرجع `null` بصمت.** لو
    صلاحية بيانات العملاء المحمية ناقصة في التطبيق، شوبيفاي بترجّع `null` **من
    غير أي خطأ** وكل المحافظات تبقى «غير محدد». اتغطّت من طبقتين: صف في
    `?action=diag` بيفحصها على أوردر حقيقي، وبانر أحمر في التاب نفسه لو **كل**
    الصفوف كده. (الحالة مش موجودة النهارده — القياس المباشر رجّع `province`
    على كل الأوردرات المفحوصة.)
  - **`MIN_WORKER_VERSION` اترفعت `3.6.0` → `3.7.0`** في نفس التسليم — قاعدة
    الصيانة الإلزامية (الفخاخ تحت). من غيرها الحارس بيسكت على Worker قديم
    والتاب بيرندر «غير محدد» لكل حاجة.
  - **التكلفة على الاستعلام: منخفضة ومقصودة.** `shippingAddress` object جوّه
    النود (مش connection، مفيش صفحات) + metafield تالت على نفس النود — صفر
    نداء إضافي وصفر endpoint جديد. (`dashboard-builder` Step 0: `shippingAddress`
    مصنّفة **منخفضة** التكلفة وبتفتح «تحليل المناطق» بالظبط.)
  - **`switchMainTab` بقت من `TAB_DEFS`.** أسماء الزراير والحاويات كانت متكررة
    حرفيًا في `switchMainTab` و`updateTabChip` و`buildTabChipMenu` — إضافة تاب
    تالت كانت هتحتاج تعديل التلاتة، ونسيان واحد = تاب بيشتغل من الزرار وبيختفي
    من قايمة الـ chip على الموبايل.

- **`In-Return` ≡ `Shipped` في S1 و S2 — قرار أحمد 25-08-2026.** الكود كان
  مطبّقها صح من الأصل؛ Rule 12 في `ecommoda-order-lifecycle` هي اللي اتوسّعت
  لتغطي S2 وعدّ الأوردرات كمان (كانت قبل كده S1 وفلوس بس). **مفيش مربع
  "قيد الإرجاع" ومفيش `IN_TRANSIT_BACK`** — أي اقتراح بإضافة واحد مرفوض بقرار.
- **صف "توقّع اكتمال الفترة" (تاب الأوردرات) — Rule 11 في
  `ecommoda-dashboard-builder`، 25-08-2026.** توقّع HTML-only مشتق بالكامل من
  `orderBoxes` الموجود أصلاً في `get_data` — صفر تعديل في الـ Worker، صفر
  `CACHE_VERSION` bump. المعدلات (`pShip`/`pDeliver`) بتتحسب من نفس الفترة
  المعروضة (قرار أحمد — حد معروف مقبول، مش باج). مفيش حد أدنى للعيّنة حاليًا
  (مؤجل بقرار أحمد).

- **دورات R/E المتعددة (G-13) — خطوة (أ) + (ب)، 26-08-2026.** نوع الدورة
  ومرحلتها وتسويتها بتتقرا من **أحدث دورة غير مُهمَلة** (مرتّبة بـ
  `returns[].createdAt`) في المكنتين، مش `.some()` على كل الدورات.
  ⚠️ **الفرع النهائي استثناء مقصود:** «الأوردر ده شاف استبدال أصلاً؟» بيفضل
  `.some()` وده الصح — تطبيق «أحدث دورة» على الفرعين بيحرّك ٩ أوردرات بدل
  واحد، تمنية منهم لمربع مش أصح. وشرط التسوية `s2 ∈ {In-Return, Returned}`
  بقى على أحدث دورة بس؛ الدورة الأقدم `CLOSED` لوحده يكفيها.
  الأثر المقيس: **أوردر واحد** اتحرّك (`#50091`: `SHIPPED_EXCHANGE` →
  `SHIPPED_RETURN`)، و`CACHE_VERSION` اترفع لـ v8.
  → `ecommoda-order-lifecycle` Rule 15 · Rule 7 · `classification-rules.md` §2-C

- **وسمَا الدورات `CYCLE_OVERLAP` و `MULTI_CYCLE` — منفصلان إجباريًا.**
  الأول مخالفة تشغيلية بتتصلح في شوبيفاي (خدمة العملاء)، والتاني حد معروف في
  الكود. دمجهم في تحذير واحد بيمسك **١١%** بس من الصفوف المحتاجة مراجعة.
  ⛔ الوسم **مبيحركش ولا رقم** — الصف بيفضل في مربعه (Rule 13).
  → `state-machines.md` §2.4

- **كاش الفترات المقفولة بقى TTL متدرّج بدل دائم — 30-08-2026.**
  `ecommoda-dashboard-builder` v2.0.0 (بند كاسر) وضّحت إن قاعدة "الفترة المقفولة
  = كاش دائم" صح بس للأحداث، وغلط لصفوف بتوصف **حالة حالية** — وسمّت الأداة دي
  بالاسم كمرشّح مباشر. `boxes`/`orderBoxes` هنا حالة أوردر حالية
  (`manual_status`/`status_2_r_e`/bucket)، فأوردر اتعمل في شهر مقفول ممكن يتحرك
  `Shipped → Delivered → Returned` بعد ما الشهر يتكاش — وكاش دائم كان بيخلّي
  الرقم يفضل واقف على الصورة القديمة للأبد من غير أي خطأ ظاهر. `cacheTtlFor()`
  اتبدلت بـ `ttlFor()` متدرّجة (900 / 21600 / 86400)، عمرها ما ترجع `null`.
  `CACHE_VERSION` اترفع لـ v9 عشان كل فترة متكاشة قديمة بكاش دائم تتحرر وتتحسب
  بالـ TTL الجديد. أُضيف كمان `?action=diag` و `?action=get_config` (كانوا
  ناقصين — `ecommoda-worker-builder` Step 5A ⑨) + trailing-slash fix على
  `SHOP_DOMAIN`. → `ecommoda-dashboard-builder` Step 3-أ/ج

- **دفعة إصلاحات من مراجعة `docs/DATA-PULL-AND-CACHE.md` قبل حذفه — 30-08-2026.**
  الملف كان بيوثّق سلوك قديم اتصلّح جزئيًا في v3.2.0 (`CACHE_VERSION`/`ttlFor`) —
  المراجعة دي قفلت باقي البنود بعد فحص الكود الفعلي (مش بس التوثيق) ومسحت
  الملف بعدها. البنود:
  - 🔴 `clear_cache` الكامل بقى بيلفّ على `cursor` بتاع `DASH_KV.list()` بدل
    نداء واحد سقفه ١٠٠٠ مفتاح — قبل كده كان بيمسح جزء ويرجّع "cleared: all".
  - 🔴 **`ROWS_MAX_DAYS = 45`** — فترة أوسع بترجع `rows`/`orderRows` فاضية +
    `rowsIncluded: false` + `rowsOmittedReason` بدل ما تستنى دورة سحب كاملة
    وتاخد خطأ حجم الكاش في الآخر بصفر أرقام. `boxes`/`orderBoxes` (الملخّصات)
    فضلوا كاملين دايمًا. ⚠️ الواجهة بتحسب الكروت (`activeBoxes`/`activePrevBoxes`)
    من `rows` دايمًا (عشان وضعي "قيمة"/"عدد قطع" يفضلوا مطابقين لجدول
    التفاصيل) — فلما `rowsIncluded=false` بترجع لـ `boxes`/`prevBoxes` مباشرة
    (نفس أسماء الحقول)، ووضع "عدد القطع" (اللي محتاج `rows` ليتحسب) بيتقفل
    مؤقتًا مع بانر بيوضّح السبب. `CACHE_VERSION` اترفع لـ v10.
  - 🟠 `MAX_CACHE_BYTES` نزلت من 25MB (نفس حد KV بالظبط) لـ 24MB.
  - 🟠 `get_data`/`get_meta` بقى فيهم فحص صيغة `YYYY-MM-DD` + `dateFrom<=dateTo`.
  - 🟠 الفترة الحالية والسابقة (`state.prevRows`) بيتجابوا تسلسليًا في الواجهة
    مش بالتوازي — تجنّب تنافس throttle على cache miss بارد للفترتين مع بعض.
    ⚠️ **الفترة السابقة اتسابت زي ما هي عمدًا** (كاملة، مش مصغّرة) — `prevRows`
    مُستخدمة فعليًا في مقارنة وضع "عدد قطع" (`activePrevBoxes`)، مش بيانات
    بتترمى زي ما كان مفترض في نسخة قديمة من الملف المحذوف.
  - 🟡 `readCache()` بقى فيها `try/catch` حوالين `JSON.parse`.
  - 🟡 `getAccessToken()` بقى بيكاش التوكن في نطاق الـ isolate.
  → `index.js` v3.3.0 · `index.html` v3.4.0
  ⚠️ **`ROWS_MAX_DAYS` بقى حارس في الـ Worker بس** بعد تعديل 30-08-2026 تحت —
  التقسيم الشهري خلّى كل نداء شهر واحد على الأكثر، فالسقف عمره ما بيتفعّل من
  الواجهة، و`rowsIncluded`/`rowsOmittedReason` اتشالوا من منطق العرض. لو رجع
  `false` رغم كده، الواجهة **بترمي بصوت عالي** بدل ما ترندر أصفار — لأن كل
  رقم على الشاشة بقى محسوب من الصفوف.

- **🔴 التقسيم الشهري + مربعات الشهور + الفترة السريعة — 30-08-2026
  (`index.js` v3.5.0 · `index.html` v3.5.0).** مراجعة كاملة مقابل
  `ecommoda-dashboard-builder` v2.1.0 · `ecommoda-html-builder` v2.1.0 ·
  `ecommoda-worker-builder` v1.1.0 · `ecommoda-constants` v1.2.0، مع طلب صريح
  من أحمد بمربعات الشهور اللي في `Logistics-Control-Center`.

  - 🔴 **شبكة الكاش ≠ الفترة المعروضة** (dashboard-builder Step 3-د-١). التحميل
    اتقسم على **شهور تقويمية كاملة بلا أي قصّ** على حدود اختيار المستخدم،
    والأطراف بتتفلتر في الواجهة بـ `createdAt` (`visibleRows`). **الفترة
    المعروضة والليبلات ما اتغيّروش إطلاقًا** — اللي اتغيّر هو أنهي مفتاح بيتقرا.
    قبل كده كل فترة كانت مفتاح مستقل: `آخر 30 يوم` → `data:…:08-01:08-30`
    مالوش أي علاقة بـ`data:…:08-01:08-31` رغم **فرق يوم واحد**، و"آخر ٧ أيام"
    كانت بتولّد مفتاح جديد **كل يوم**. دلوقتي التلاتة بيقروا مفتاح أغسطس نفسه.
  - 🔴 **الشبكة هجينة، مش مطلقة** (نفس البند، الخيار التالت "الأفضل"). الأداة
    دي افتراضها `اليوم` — إخضاعها لشبكة شهرية مطلقة معناه سحب شهر كامل عشان
    تعرض يوم وإعادته كل ربع ساعة (TTL الشهر المفتوح). القاعدة المطبّقة:
    مفتاح الشهر موجود → اقرا منه وفلتر محليًا · مش موجود والفترة ≤ ٣١ يوم ومش
    على حدود شهر → اسحب الفترة نفسها بمفتاحها (`SHORT_RANGE_MAX_DAYS`).
  - 🔴 **البوابة بالتكلفة مش بوجود نقص** (Step 3-د-٢). `GATE_MIN_MISSING_MONTHS = 1` —
    شهر واحد ناقص بيتحمّل تلقائيًا بشريط تقدّم، البوابة من شهرين فما فوق.
    ⛔ **الفترة القصيرة (chunk بحدودها هي) مستثناة من العدّ تمامًا** — الـ miss
    فيها هو الوضع الطبيعي وتكلفته يوم مش شهر؛ بوابة عليها = زرار "تطبيق" على
    "اليوم"، وده بالظبط اللي `date-range-filter.md` §1 و§5 شايلينه.
    ⛔ علاج البوابة بإطالة TTL الـ `meta` **مرفوض صراحةً** — بيخلي الـ probe
    يقول "متكاش" وهو مش متكاش (استبدال إزعاج بكذب).
  - 🆕 **مربعات الشهور** (Step 3-د-٣ · `monthly-chunk-loading.md` §9). صف
    **مستقل فوق** صف الفلتر — الترتيب الإلزامي الداخلي (`فترة سريعة ← من ← إلى`)
    **ما اتغيّرش**، والمربعات **مش بديل** للفترة السريعة (`date-range-filter.md`
    §1a). الشهر الحالي + ٥ سابقين، الأحدث أولاً = أقصى اليمين في RTL. المربّع
    بياخد **الشهر التقويمي كامل** حتى الشهر الحالي (مش "لحد النهارده" — ده كان
    هيولّد مفتاح جديد كل يوم ويلغي السبب الوحيد لوجود المربّع). الضغطة =
    **سحب مباشر من شوبيفاي** (الاستثناء الموثّق الوحيد، "الضغطة *هي* الزرار")
    ومعاها **حارس حداثة** ~~`MONTH_BOX_FRESH_MINS = 5`~~ → **`= 15` من
    01-09-2026** (مربوطة بـ `TTL_OPEN` — شوف مراجعة v3.8.0 فوق).
    المربّع والفترة السريعة
    **بيمسحوا بعض** (`resetRangePresetUI` ↔ `clearActiveMonthBox`).
    ⚠️ **الافتراضي فضل `اليوم`** — مربّع الشهر كافتراضي قرار صاحب الأداة، ما
    اتخذش هنا.
    ~~ومفيش `STORE_START_DATE` في الأداة دي، فالمربعات مش مقصوصة على أي حد
    أدنى~~ — **اتغيّر 31-08-2026:** المربعات بقت مقصوصة على `STORE_START_YM`
    (شوف قرار `STORE_START_DATE` تحت).
  - 🔴 **كل رقم على الشاشة بقى محسوب من الصفوف المعروضة.** `orderBoxes` و
    `warnings` و`orderWarnings` و`ordersScanned` و`candidatesFetched` كانوا
    بيتقروا من رد الـ Worker جاهزين — ومع التقسيم الشهري دول بيوصفوا الـ chunk
    (شهر كامل) مش الفترة المعروضة، يعني كارت بيعدّ أوردرات مالهاش صف على
    الشاشة. اتضافت `computeOrderBoxesFromRows` و`computeWarningsFromRows`
    و`computeOrderWarningsFromRows` جنب `computeBoxesFromRows` الموجودة أصلاً.
    وعشان `candidatesFetched` تتحسب لأي **جزء** من الفترة، الـ Worker بقى
    بيحط `hasRE` على كل صف أوردر → `CACHE_VERSION` v10 → **v11**.
  - 🔴 **مسح كل `sleep` ثابت بين دفعات الجلب** (Step 5). كان فيه `sleep(1000)`
    بين صفحات المرحلة 1 و`sleep(500)` بين دفعات المرحلة 2 — ٣ آلاف أوردر في
    الشهر ≈ **٢٧ ثانية نوم مقصود**، ومدة الطلب بقت دالة في عدد الصفحات مش في
    سرعة شوبيفاي. الانتظار الوحيد الباقي هو backoff الـ `THROTTLED` في
    `shopifyWithRetry`. التقسيم الشهري هو **العلاج الهيكلي** اللي المهارة
    بتسمّيه، واتنفّذ في نفس التسليم.
    ⚠️ سطر «ثانية بين الصفحات» في Data Contract v2.1.0 §6 اتعلّم عليه كمنسوخ
    **هناك** — الثوابت المقفولة في نفس القسم ما اتلمستش، ودي مش واحدة منهم.
  - 🆕 **زرارا تحديث** (`تحديث الشهر الحالي` / `تحديث كل الشهور` بتأكيد وبيختفي
    لو الفترة شهر واحد) + **سطر «طزاجة كل شهر»** تحت المؤشر الرئيسي — المؤشر
    بيعرض **أقدم** شهر (الأكثر تحفّظًا)، والسطر بيفكّه لكل شهر. الاتنين إلزاميين
    مع بعض (`monthly-chunk-loading.md` §6/§8). المؤشر وزراريه اتنقلوا من الهيدر
    لصف الفلاتر ككتلة واحدة (`dashboard-ui-patterns.md` §1/§9).
  - 🆕 **`runDiag()` و`checkWorkerVersion()`** — `?action=diag` و`?action=get_config`
    كانوا موجودين في الـ Worker من v3.2.0 **من غير أي مستهلك في الواجهة**، يعني
    الفحص اللي بيكشف `DASH_KV` binding ناقص محدش كان يقدر يشغّله
    (`worker-builder` v1.1.0: الشرط بقى «بيكتب **أو بيكاش**»).
    ⚠️ ~~تحذير النسخة بيقارن مع `EXPECTED_WORKER_VERSION` **مش** `TOOL_VERSION`~~
    — **اتغيّر 01-09-2026:** المقارنة بقت `cmpVersion(workerVersion,
    MIN_WORKER_VERSION) < 0`، يعني «الـ Worker جديد كفاية؟» مش «متطابق؟».
    التطابق الحرفي مع `EXPECTED_WORKER_VERSION` كان لسه بيولّع تحذير كاذب دايم
    لأن الرقمين مستقلين بالتصميم. شوف مراجعة v3.8.0 فوق.
  - 🟡 **البيانات بتتحمّل بعد تسجيل الدخول** مش قبله — قبل كده دورة سحب كاملة
    كانت بتشتغل ورا شاشة الدخول حتى لو محدش دخل، ومع التقسيم الشهري شريط
    التقدّم والبوابة كانوا هيرسموا تحت الـ overlay.
  - 🟡 `updateLoginSettingsBtnState` → `updateLoginSettingsBtn` (الاسم القياسي —
    `html-builder` Step 4). كان متسمّي بالاسم القديم في التعريف والنداء مع بعض
    فما كانش بيرمي، بس بيخالف القالب.
  → `index.js` v3.5.0 · `index.html` v3.5.0

- **🔴 مراجعة على أحدث المهارات — 01-09-2026 (`index.js` v3.6.2 ·
  `index.html` v3.8.0).** الأداة كانت متوقّفة على `html-builder` v2.2.0 ·
  `worker-builder` v1.1.0 · `dashboard-builder` v2.1.0 · `order-lifecycle`
  v1.1.0 · `constants` v1.4.1، والمهارات وصلت v6.0.0 / v2.0.0 / v3.1.0 /
  v1.2.0 / v1.4.3. البنود المنطبقة اتنفّذت كلها، والباقي اتكتب هنا **بسبب**
  عدم انطباقه بدل ما يفضل سؤال مفتوح للجلسة الجاية.

  - **🔴 باند قفل الهيدر بقى مقيس مش رقم ثابت** (`html-builder` v6.0.0 ·
    Standards #33 · `tabs-and-modals.md` §1b). إصلاح 30-08-2026 (عتبتين
    12/40 + rAF) **خفّف** الفليكر وما قفلهوش. السبب: قفل الهيدر بيقلّل ارتفاعه
    **فوق منطقة العرض**، والمتصفح (scroll anchoring) بيحافظ على المحتوى مكانه
    فبيطرح الفرق من `scrollY` نفسه — يعني الفعل بيحرّك شرطه بقفزة أكبر من
    الباند كله، والـ rAF بيبطّأ اللوب مش بيمنعه.
    **القياس على الأداة دي (Chromium، عدّاد تبديلات `.stuck` على الملف
    الفعلي):** فرق الارتفاع **71px** والباند كان **28px** — يعني الارتداد
    ضِعف الباند ونص. سكرول بطيء من أول الصفحة **10 تبديلات → 1** · ذبذبة
    حوالين نقطة القفل **14 → 1** · والسلوك الصح ما اتغيّرش (نزول ورجوع لفوق
    = 2 في الاتنين). `measureSticky()` بتقيس الفرق (بالترانزيشن مطفي وفي نفس
    الفريم) و`STICK_ON = max(40, stickyDelta + STICK_OFF + 24)`، وإعادة القياس
    على تغيّر **العرض** بس بـ debounce، والنداء في الـ Init **بعد** رسم
    التابات والـ chip.
    ⛔ البدائل المرفوضة (`overflow-anchor: none` · قفل زمني · حارس "الصفحة
    قصيرة" · رقم ثابت أكبر) مكتوبة في المهارة — متتضافش تاني كـ"احتياط".

  - **🔴 حارس نسخة الـ Worker بقى حد أدنى بدل تطابق حرفي** (`html-builder`
    v4.0.0 · Standards #29). `EXPECTED_WORKER_VERSION` + `!==` كان بيقارن
    رقمين **مستقلين بالتصميم** في الأداة دي — واختلافهم هو الوضع الطبيعي
    الموثّق في نفس الملف ده. يعني التحذير كان مولّع على طول، والموظف بيتجاهله،
    فالحالة اللي الزرار موجود عشانها (Promote ناقص · rollback · بناء فاشل)
    بقت بلا أي إشارة. حماية وهمية أسوأ من مفيش حماية.
    الجديد: `MIN_WORKER_VERSION = '3.6.0'` (النسخة اللي ضافت `storeStartDate`
    في `get_config` — أحدث حاجة الواجهة معتمدة عليها) + `cmpVersion()` رقمية،
    والأسماء الموحّدة `verStaleBtn` / `showWorkerStale()`.
    ⚠️ **فحص `storeStartDate` اتساب زي ما هو ومقصود:** دي **مش** مقارنة نسخة
    — ثابت متعاقد عليه لازم يتطابق بالحرف بين الطبقتين، فمقارنته بالتساوي
    عمرها ما بتولّع كاذب. الزرار الواحد بيغطي الحالتين وليبله بيتغيّر بحسب
    اللي ولّع.
    ⚠️ **صيانة:** أي endpoint/حقل جديد الواجهة تعتمد عليه → ارفع
    `MIN_WORKER_VERSION` لنسخة الـ Worker بتاعته **في نفس التسليم**.
    الاختبار اتعمل في الاتجاهين (Worker v3.6.1 → مخفي · v3.5.0 → ظاهر ·
    `storeStartDate` متفرّق → ظاهر بليبل تاني · رجوع للطبيعي → مخفي).

  - **🟡 `MONTH_BOX_FRESH_MINS` من ٥ لـ ١٥ = `TTL_OPEN`** (`dashboard-builder`
    v3.1.0 · `html-builder` v5.0.0). رقمين مستقلين كانوا بيجاوبوا على نفس
    السؤال ("البيانات دي طازة؟")، والنافذة بينهم **سحب مضمون بلا أي فايدة**:
    الكاش لسه حي وصالح والضغطة بتفرض `forceRefresh:true` فيرجّع الـ Worker
    **نفس** الصفوف بعد دورة GraphQL كاملة.
    ⚠️ **الأثر مش رقم أكبر، هو تضييق مدى الاستثناء:** مفتاح كاش موجود = عمره
    بالتعريف أقل من TTL بتاعه، فعلى **الشهر المفتوح** المربّع بقى كاش-أولاً
    فعليًا، والسحب الفوري بقى مسؤولية `refreshData()` (`mode:'open'` — بيفرض
    دايمًا بلا حارس). على الشهور المقفولة (TTL ٦/٢٤ ساعة) الاستثناء قايم زي
    ما هو. مفيش قدرة اتفقدت — اتنقلت للزرار.
    ⛔ وممنوع "علاج" التناقض بتقصير `TTL_OPEN` — بيزوّد السحب بدل ما يقلله.

  - **🟡 G-14 اتسجّلت في الكود — `PARTIALLY_FULFILLED` ملغي** (`order-lifecycle`
    v1.2.0). `isCancelledOrRTO()` بتسأل عن `FULFILLED` **بالظبط**، فأوردر ملغي
    وجزء من بضاعته طالع برّه المخزن بيتصنّف **إلغاء نضيف** — والحدث بيختفي من
    تقارير الـ RTO خالص بدل ما يقع في دلو غلط ويبان. جرد على المتجر
    (01-09-2026): `fulfillment_status:partial status:cancelled` رجّع **صفر**
    أوردر، فالحالة نظرية لحد دلوقتي.
    ⛔ **قرار أحمد: تتسجّل بس.** `isCancelledOrRTO()` ما اتلمستش — تحريك دلو
    بيغيّر أرقام تاريخية في كل أداة في الستاك ومحتاج قرار مستقل (Rule 13:
    علّم، ماتحركش). التحذير اتكتب فوق الدالة نفسها، والبند مفتوح تحت.

  - **البنود اللي مش منطبقة — اتفحصت مش اتفترضت:**
    · `worker-builder` v2.0.0 🔴 «انتظار التأكيد بعد ميوتيشن غير متزامنة» —
      الأداة **قراءة فقط**، صفر `mutation` في `index.js`.
    · `worker-builder` v2.0.0 🔴 «`get_logs_export` يرجّع cap/total/truncated»
      و🟡 «فلاتر السجل قوايم» + `html-builder` v5.0.0 (نفس البندين من ناحية
      الواجهة) — **مفيش Log Tab ولا أي endpoint بيقرا السجل هنا**؛ الأداة
      بتكتب في D1 بس (`login` · `logout`).
    · `html-builder` v5.1.0 🟡 «حارس `document.contains` على multi-select» —
      مفيش أي multi-select في الأداة دي.
    · `html-builder` v3.0.0 / `dashboard-builder` v3.0.0 🔴 «`visibleRows` بتوقيت
      UTC» — **اتقفل خلاص** في v3.7.0 (البند اللي تحت).
    · `constants` v1.4.2/v1.4.3 ⚪ — تحريري بحت، الجدول هو اللي اتحدّث؛ قيم
      `tool`/`type` هنا (`performance_dashboard` · `login`/`logout`) ما اتغيّرتش.

  - **⚠️ صفر `CACHE_VERSION` bump** (فضلت `v11`): كل اللي اتغيّر عرض وحراسة
    وتوثيق — لا شكل الاستعلام ولا شكل الصف ولا أي منطق تصنيف اتلمس.

- **🔴 فلترة أطراف الفترة بقت بتوقيت القاهرة بدل UTC — 01-09-2026
  (`index.html` v3.7.0).** `visibleRows()` (اتضافت مع التقسيم الشهري في v3.5.0)
  كانت بتقارن `createdAt.slice(0,10)` — وده **تاريخ UTC** — بحدود الفترة، وهي
  متولّدة بـ `cairoDateStr(cairoNow())` يعني **تواريخ قاهرة**. والعمود "التاريخ"
  في جدول التفاصيل بيتعرض بـ `fmtRowDate` = قاهرة كمان. يعني طبقة الفلترة كانت
  بتشتغل بتقويم مختلف عن اللي حواليها.
  **الأثر:** كل أوردر اتعمل بين ١٢ و٣ الفجر بتوقيت القاهرة (= ٢١:٠٠–٢٤:٠٠ UTC
  لليوم اللي قبله) كان بيتشال من اليوم اللي هو فيه فعلًا وينضم لليوم اللي قبله.
  على فترة "اليوم" ده ~٣ ساعات من أوردرات اليوم ناقصة + تسرّب من الأمس. وبما إن
  **كل رقم على الشاشة** بيتحسب من الصفوف دي (v3.5.0)، الأثر كان على الكروت
  والدونات والتحذيرات وسطر "تم فحص N أوردر" مع بعض.
  ⚠️ **الهوية الحسابية مكانتش بتكشفه** — الطرفين بيتحسبوا من نفس الصفوف
  المفلترة غلط، فبتفضل متوازنة على رقم ناقص.
  **الدليل (قياس مباشر على شوبيفاي، 01-09-2026):** `shop.ianaTimezone` =
  `Africa/Cairo`، وفلتر `created_at:>=2026-08-25 created_at:<=2026-08-25` رجّع
  أوردرات من `#51989` (`2026-08-24T21:06Z`) لحد `#52119` (`2026-08-25T20:59Z`)
  — يعني شوبيفاي بتقطع اليوم بتوقيت المتجر، والواجهة بقت بتطابقها بالظبط.
  **⚠️ صفر `CACHE_VERSION` bump** (فضلت `v11`): الإصلاح في العرض بس — لا شكل
  الاستعلام ولا شكل الصف ولا أي منطق تصنيف اتغيّر.
  ℹ️ نفس القياس أثبت إن `created_at:<=D` بالتاريخ المجرّد **شامل اليوم كله**،
  فصيغة `query-cost-guide.md` §6 (`…T23:59:59`) مش لازمة هنا و**الاستعلام في
  §6 من الـ Data Contract متتغيّرش**.

- **🟡 `WORKER URL` و`ADMIN WORKER URL` بقوا constants — 01-09-2026
  (`ecommoda-html-builder` v2.2.0 · Standards Changelog #28).** الاتنين اتشالوا
  من شاشة الإعدادات ومن `localStorage`. الرابط مش سر (الحماية في `WORKER_SECRET`
  والبوابة على كل راوت، واسم الـ Worker بيتحدد وقت الإنشاء ومايتغيّرش)، فتحويله
  لحقل إدخال كان بيحمّل الموظف خطوة إعداد بلا مكسب أمني — وبيخلي "الأداة مش
  شغّالة" غالبًا رابط متكتب غلط. `isConfigured()` بقت على السر وحده،
  و`LS_SECRET` بقى **مفتاح الـ localStorage الوحيد**. الرابط لسه بيتعرض للقراءة
  في شاشة الإعدادات (`cfgUrlView`) عشان الدعم والفحص الذاتي.
  ⚠️ `ADMIN_WORKER_URL` مكتوبة من `ecommoda-constants` §5b بس **مفيش مستهلك
  ليها في الأداة دي** (مفيش زرار "إدارة الموظفين") — موجودة عشان أي إضافة جاية
  تلاقيها في مكانها القياسي بدل ما تخترع واحدة.
  📌 المهارة بتقول «الأدوات القديمة تتحوّل عند **أول تعديل حقيقي**» — ده كان هو.

- **🟡 `diag`: `LOCATION_ID` بقى صف معلوماتي — 01-09-2026 (`index.js` v3.6.1).**
  المتغيّر ده **مش مستخدم في كود الأداة دي ولا مرة**، وكان غيابه بيقلب `ok`
  العام لـ `false` فالفحص يقول "فيه فحوصات فاشلة" على حاجة مالهاش أي أثر —
  إنذار كاذب بيدفن تحته الفحوصات الحقيقية (زي `DASH_KV` binding ناقص، اللي هو
  السبب الوحيد لوجود `diag` هنا أصلاً). `SHOP_DOMAIN` وحده بقى المتغيّر الحاسم.

- **🔴 `STORE_START_DATE = 2026-05-01` — الأداة بتبدأ من بداية تشغيل المتجر،
  31-08-2026 (`index.js` v3.6.0 · `index.html` v3.6.0).** قرار أحمد: اللي قبل
  01-05-2026 يتعامل معاه **كأن المتجر ماكانش شغّال** — مفيش سحب، مفيش كاش،
  ومفيش رقم على الشاشة. البند ده كان مفتوح في «مسائل مفتوحة» بشرط صريح إنه
  «لو اتقرر، لازم يتحط في `index.js` **و** `index.html` في نفس التعديل» —
  والشرط ده اتنفّذ.

  - **الطبقتين مختلفتين في الوظيفة عن قصد.** الـ Worker **بيرفض** (400 من
    `validateDateRange` — choke point واحد بيغطّي `get_data` و`get_meta`)،
    والواجهة **بتقصّ قبل ما تسأل** (`clampRangeToStoreStart`) عشان المستخدم
    يشوف في حقلي من/إلى الفترة اللي هياخدها بالظبط. الواجهة هي واجهة
    الاستخدام، والـ Worker هو الحارس الأخير.
  - **⛔ الـ Worker بيرفض ومبيقصّش.** مفتاح الكاش هو **نصّ التاريخين حرفيًا**،
    فقصّ صامت جوّه الـ Worker معناه إما نفس المحتوى تحت مفتاحين (نسختين
    بيتحدّثوا مستقلين)، أو رد على فترة **غير** اللي الواجهة سألت عنها —
    فالـ probe بتاعها (`get_meta` بالتواريخ الأصلية) يفضل يقول «مش متكاش»
    للأبد ويسحب كل مرة. الرفض بصوت عالي أنضف من قصّ صامت.
  - **⚠️ المقارنة (Δ%) بتتلغي، مبتتقصّش.** `getPreviousRange` بترجّع **`null`**
    لو الفترة السابقة مش داخلة **كلها** بعد الحد. قصّها كان هيقارن نافذتين
    بأطوال مختلفة — «آخر ٩٠ يوم» (٩٠ يوم) مقابل ٣٣ يوم بس — وده رقم غلط شكله
    سليم، أخطر من غياب المقارنة. `delta()` بترجّع `''` لوحدها لما `prevVal = 0`
    فالمربعات بتفضل بأرقامها والسطر بس هو اللي بيختفي. الأثر العملي: مربّع
    «مايو 2026» ومربّع «العام الحالي» (المقصوص) مالهمش Δ%.
  - **الأثر على الواجهة:** `min` على حقلي التاريخ · `computeMonthBoxes` بتقف
    عند `STORE_START_YM` (٤ مربعات بدل ٦ دلوقتي) · `markUnavailablePresets`
    بتعطّل أي preset مداه كله قبل الحد («العام السابق» حاليًا) · «شهر محدد»
    بترفض شهر قبل الحد وسنينه مقصوصة على `2026` · `monthChunks`/`monthBoxRange`
    بيقصّوا على الحد كمان (**بلا أثر النهارده** لأن الحد أول يوم في شهر — ده
    حارس لو الحد اتنقل لنص شهر، ساعتها الشهر كان هيتولّد بـ `-01` والـ Worker
    كان هيرفض النداء).
    ⚠️ القصّ ده على **ثابت** مش على حدود اختيار المستخدم، فقاعدة «ممنوع قصّ
    الـ chunk» سليمة: المفتاح بيفضل هو هو لكل فترة بتلمس شهر الحد.
  - **`get_config` بقى بيرجّع `storeStartDate`** و`checkWorkerVersion()` بتقارنه
    بثابت الواجهة وبتعلن الاختلاف زيه زي اختلاف النسخة — من غير كده، نشر طبقة
    واحدة بس بيسيب الأداة شكلها سليم والفلترة شغالة في نص واحد.
  - **⚠️ صفر `CACHE_VERSION` bump** (فضلت `v11`): لا شكل الاستعلام ولا شكل الصف
    ولا أي منطق تصنيف اتغيّر. المفاتيح المتكاشة لفترات قبل الحد بقت غير مقروءة
    بس وبتنتهي بالـ TTL لوحدها — bump كان هيرمي كاش **صحيح** لكل الفترات بعد
    الحد بلا سبب.
  - **الافتراضي فضل `اليوم`** — الحد مالوش علاقة بالافتراضي، وتغييره لسه قرار
    صاحب الأداة (زي ما هو مكتوب في v3.5.0 فوق).

## خط الأساس بعد النقل

> ⚠️ **لسه متسجّلش.** أول ما الأداة تتأكد إنها شغالة بعد أول نشر من git، سجّل هنا
> أرقام فترة معروفة (`get_data` لفترة مقفولة) — من غيرها مفيش إثبات إن النقل نضيف.

```
(فاضي)
```

## إثبات إن النقل نضيف

> ⚠️ الأرقام دي بصمة **وقت النقل**. `index.js` اتغيّر بعدها مرتين (تطبيع
> CRLF→LF في `f69884f`، وبعدين v3.1.0)، و`index.html` اتغيّر في v3.2.0 —
> فالبصمتين مش المفروض يطابقوا دلوقتي. مرجع المطابقة الحالي هو git نفسه.

```
index.js    md5 b9cb0839cff7387091fa252e1a14decf   ← مطابق لنسخة كلاودفلير بايت ببايت
index.html  blob SHA 15e3af845754036eb42875c594c1c6f127eff405
            ← نفس الـ blob SHA بتاع Index.html القديم قبل الرينيم = صفر تعديل
```

## فخاخ الأداة دي

- **`DASH_KV` هو أخطر حاجة في النقل ده.** `wrangler deploy` تعريفي: أي binding مش
  مكتوب في `wrangler.toml` **بيتشال** عند أول نشر من git، حتى لو كان متضاف يدويًا
  في الداشبورد. لو `DASH_KV` اتشال → `env.DASH_KV` = `undefined` → `get_data`
  بيرمي على كل نداء. الـ binding والـ id الحقيقي متكتبين في `wrangler.toml` —
  **ما تشيلهمش ولا تعدّلهم**.
- **`clear_cache` من غير `dateFrom`/`dateTo` بيمسح كاش الأداة كله** (`list` بـ prefix
  `dash:performance_dashboard:` ثم `delete` على كل مفتاح). مش destructive — بيعاد
  حسابه — بس بيكلّف نداءات GraphQL كاملة لكل فترة بعدها.
- **`index.html` بحروف صغيرة إجباري.** GitHub Pages بيدوّر على `index.html` كملف
  افتراضي للمجلد — `Index.html` بحرف كبير بيدي **404** على الرابط المختصر.
  الـ `Index.html` الموجود دلوقتي **صفحة تحويل بس** (`meta refresh`، صفر منطق) —
  متحطش فيه أي كود.
- **الأسرار متتكتبش في الريبو أبدًا.** `CLIENT_ID` · `CLIENT_SECRET` · `WORKER_SECRET`
  موجودين في الداشبورد وهيفضلوا. وبعد أي سر جديد → **Promote**.
- **🔴 ممنوع ترجّع قصّ الـ chunk على حدود الفترة المختارة.** الشكل `first > from ? …`
  / `last < to ? …` بيبان "أنضف" لأنه بيسحب المطلوب بالظبط — وهو اللي بيكسر
  كل فايدة التقسيم: مفتاح الكاش هو **نصّ التاريخين حرفيًا**، فأي فترة مش مطابقة
  لحدود شهر بتولّد مفتاح يتيم، و"آخر ٧ أيام" بتولّد واحد **كل يوم**. الأطراف
  بتتقصّ في `visibleRows()` بس. → `dashboard-builder` Step 3-د-١.
- **🔴 `STORE_START_DATE` في مكانين — لازم يتحرّكوا مع بعض.** القيمة
  (`2026-05-01`) مكتوبة في `index.js` **و** `index.html`. نشر واحدة من غير
  التانية بيسيب الأداة شكلها سليم والفلترة شغالة في طبقة واحدة بس: الواجهة
  لوحدها = الـ Worker لسه بيسحب لو حد نده الـ endpoint مباشرة؛ الـ Worker
  لوحده = الواجهة بتعرض فترة وبتاخد 400 عليها. `get_config` بيرجّع القيمة
  و`checkWorkerVersion()` بتقارنها وبتحذّر — بس ده كشف، مش منع.
- **🔴 أي مقارنة تواريخ في الواجهة لازم تبقى بتوقيت القاهرة.** الأداة كلها
  بتشتغل بتقويم القاهرة: حدود الفترة (`cairoDateStr(cairoNow())`)، العمود
  "التاريخ" (`fmtRowDate`)، ومربعات الشهور — و**شوبيفاي نفسها** بتفلتر
  `created_at:` بتوقيت المتجر (`Africa/Cairo`). `createdAt` جاي بـ `Z`، فـ
  `.slice(0, 10)` عليه بيدي **تاريخ UTC** — يوم مختلف لـ ٣ ساعات كل ليلة.
  ده بالظبط اللي كسر `visibleRows()` من v3.5.0 لحد v3.7.0. الصيغة الصح:
  `cairoDateStr(toCairo(iso))`. ⚠️ النموذج في
  `ecommoda-html-builder/references/monthly-chunk-loading.md` §3 بيستخدم
  `.slice(0,10)` المجرّدة — **متنسخهاش هنا زي ما هي**.
- **🔴 `STICK_ON` بتتقاس، مابتتكتبش.** أي رجوع لرقم ثابت (`const STICK_ON = 40`
  أو أي رقم أكبر) = باج الفليكر راجع. فرق ارتفاع الهيدر المقيس في الأداة دي
  **71px** — أوسع من أي باند ثابت اتكتب هنا قبل كده. والسبب هيكلي مش تجميلي:
  القفل بيغيّر `scrollY` نفسه (scroll anchoring)، فالفعل بيغذّي شرطه.
  ⛔ ومعاه: ممنوع `overflow-anchor: none`، وممنوع قفل زمني حوالين التبديل،
  وممنوع حارس "الصفحة قصيرة" — التلاتة اتقاسوا في المهارة و`صفر فرق` أو أثر
  جانبي أسوأ. → `ecommoda-html-builder` `tabs-and-modals.md` §1b.
- **🔴 المحافظة من `province` مش `city` — والتجميع على الاسم مش الكود.**
  `city` حقل حر بيكتبه العميل: «القاهرة» و«6اكتوبر» و«فيصل» و«الشروق» كلهم
  أوردرات قاهرة بقيم `city` مختلفة (قياس مباشر 01-09-2026). التجميع عليها
  بيولّد عشرات الصفوف لنفس المحافظة وجدول بلا معنى.
  ⛔ وممنوع تحويل `PROVINCE_AR` لخريطة `provinceCode`: اسم مش في الخريطة
  بيتعرض **خام** فالنقص بيبان، لكن كود غلط في خريطة أكواد بيتعرض **باسم محافظة
  تانية** — نفس عيلة «رقم غلط شكله سليم».
- **🔴 `prov`/`zone` على الصف، ممنوع تتجمّع في الـ Worker.** أي `provinceCounts`
  أو ما شابه في الـ payload بيوصف **الشهر التقويمي الكامل** مش الفترة المعروضة
  (نفس فخ `orderBoxes` اللي اتصلّح في v3.5.0). الحقلين على الصف عشان أي جزء من
  الفترة يتحسب صح بعد `visibleRows()`.
- **🔴 أرقام تاب المحافظات من `computeBoxesFromRows`/`computeOrderBoxesFromRows`
  بس.** ممنوع تعريف تاني لـ«صافي المبيعات» أو «تسليمات» جوه `§GEO-JS` — التعريف
  المكرر بيتفرّق عن أصله عند أول تعديل في التصنيف، والفرق بيبان في مجموع عمود
  في تاب مقابل كارت في تاب تاني، وده آخر مكان حد بيبص فيه.
- **🔴 «قيد التنفيذ» في تاب المحافظات بيتحسب بالطرح — ممنوع تحويله لجمع دلاو
  بالاسم.** `الإجمالي − تسليمات − (إلغاءات ومرتجعات)` في `geoRow`. جمع الدلاو
  بالاسم بيبان "أوضح" وهو اللي بيكسر الأداة بصمت: أول bucket جديد يتضاف في
  الـ Worker بيقع برّه التلات أعمدة، فمجموعهم يبقى أقل من الإجمالي **من غير أي
  خطأ ظاهر**. نفس عيلة «رقم غلط شكله سليم».
- **🔴 الرسمين في كل قسم لازم يفضلوا بنفس ترتيب المجموعات.** الاتنين بياخدوا
  `rows` واحدة من `geoAllRows`. ترتيب مستقل لكل رسم (كل واحد بمقياسه) بيخلي
  المقارنة الرأسية — اللي هي **السبب الوحيد** لوجودهم فوق بعض — كذب.
- **🔴 ممنوع `scrollIntoView` (ولا أي تمرير تلقائي) عند الضغط على صف/عمود/مربع.**
  الضغطة فعل فلترة، والقفزة بتودّي المستخدم بعيد عن اللي بيقارن فيه. القاعدة
  دي اتطبّقت على تاب المبيعات وتاب الأوردرات قبل كده، وعلى تاب المحافظات في
  02-09-2026 — يعني **مفيش أي `scrollIntoView` في الملف كله دلوقتي**.
- **🔴 ممنوع ليبلات مايلة (`maxRotation > 0`) في أي bar chart.** قرار أحمد
  02-09-2026. لو الليبلات مش عاملة، الحل هو **زيادة `GEO_GROUP_W`** (والسكرول
  الأفقي) أو الفلترة — مش تمييل الليبل. و`GEO_GROUP_W` مشتق من أعرض ليبل مقيس،
  فأي اسم أطول يتضاف للقايمة لازم يتقاس تاني.
- **🔴 فلتر المحافظات بيفلتر الصفوف الخام قبل أي تجميع.** ممنوع نقله لطبقة
  المجموعات: قسم المناطق بيتجمّع على `zone` مش `prov`، فكان هيفضل بيعدّ كل
  الأوردرات والصفحة تعرض رقمين بمقاسين مختلفين من غير إشارة. وأي قسم جديد
  يتضاف للتاب لازم يقرا من `geoFilterRows()` مش من `state.rows` مباشرة.
- **⚠️ `.geo-chart-scroll` لازم تفضل `direction: ltr`.** شيلها = حاوية RTL
  بتبدأ مسكرولة على أقصى اليمين، فأول مشهد يبقى ذيل المحافظات الفاضية والمحور
  الرأسي مقصوص برّه الشاشة. ومعاها: لاحقة تيكات المحور **لاتينية** (`K`/`M`) —
  خلط رقم لاتيني بلاحقة عربية في `fillText` بيقلب ترتيبهم (bidi).
- **🔴 `MIN_WORKER_VERSION` صيانتها إلزامية.** أي endpoint أو حقل جديد في الـ
  Worker الواجهة تعتمد عليه → **ارفع القيمة في نفس التسليم**. لو نسيت، الحارس
  بيرجع لوضع «مفيش حماية» (بيسكت على Worker قديم ناقص الحاجة الجديدة).
  ⛔ وممنوع تزامنها مع `TOOL_VERSION` — ده بالظبط الباج اللي اتصلّح في v3.8.0:
  رقمين مستقلين بالتصميم، ومقارنتهم بالتساوي = تحذير كاذب دايم يتعوّد الكل
  يتجاهله.
- **🔴 ممنوع أي `await sleep(N)` غير مشروط في مسار الجلب.** الانتظار الوحيد
  المسموح هو backoff الـ `THROTTLED` جوه `shopifyWithRetry`. النوم الثابت
  بيخلي مدة الطلب دالة في **عدد الصفحات** مش في سرعة شوبيفاي، وبيبان زي
  "احترام للـ rate limit" وهو مش كده. → `dashboard-builder` Step 5.

## استرجاع النسخ القديمة

> ده بديل الـ tags — دفع الـ tags ممنوع من جلسات Claude Code السحابية.

```
النسخ المرقّمة (2.0 · 2.1.0 · 2.1.1 · 2.1.2 · 2.1.3) و Index.html الأصلي محفوظين
في commit: 30ce4b2
git show 30ce4b2:2.1.3.html
git show 30ce4b2:Index.html
```

الملفات المرقّمة **اتمسحت من `main`** — كانت صفحات لايف مفتوحة للعالم على
`.../Performance-Dashboard/2.1.3.html` بمنطق قديم بيضرب نفس الـ Worker الحالي.

## مسائل مفتوحة

- ~~**تسجيل `performance_dashboard` في `ecommoda-constants` §7**~~ — **اتقفل.**
  المهارة في `v1.2.0` بقى فيها الصف
  `| Performance Dashboard | performance_dashboard | login · logout ✅ |`،
  متأكَّد من كود الـ Worker المنشور ومن صفوف D1 الفعلية (§7 + الملحوظة تحت
  الجدول). مفيش أي `type` تالت في الكود — اتأكد بـ `grep` على كل `writeLog(`
  في `index.js` في مراجعة 30-08-2026.
- ~~**مفيش `STORE_START_DATE` في الأداة دي**~~ — **اتقفل 31-08-2026 بقرار أحمد.**
  القيمة `2026-05-01` (نفس قيمة `Logistics-Control-Center`) اتحطّت في `index.js`
  **و** `index.html` في نفس التعديل زي ما البند ده كان بيشترط. التفاصيل فوق في
  «قرارات معتمدة».
- ~~**حد معروف:** تصنيف أوردر بأكتر من دورة R/E واحدة مش دقيق~~ — **اتقفل
  26-08-2026** (G-13 خطوة أ + ب، فوق في «قرارات معتمدة»). ما احتاجش Data
  Contract جديد: `returns[].createdAt`/`closedAt` سكالر على نود بيتجاب أصلاً،
  فالتكلفة صفر. **الباقي:** خطوة (ج) `orderCycleRows[]` — مؤجّلة بقرار، وهي
  اللي هتخلي الدورة الأقدم المكتملة ظاهرة في الـ KPIs بدل ما المربع الواحد
  يوصف أحدث دورة بس.
- **🟡 G-14: أوردر ملغي حالته `PARTIALLY_FULFILLED` بيتعدّ إلغاء نضيف.**
  `isCancelledOrRTO()` بتسأل عن `FULFILLED` بالظبط، فالحالة دي بتروح
  `CANCELLED` — يعني أوردر اتلغى وجزء من بضاعته طالع برّه المخزن بيختفي من
  تقارير الـ RTO خالص. **الجرد (01-09-2026): صفر أوردر** بالحالة دي في المتجر،
  فالفجوة نظرية لحد دلوقتي — وعشان كده اتسجّلت بدل ما تتصلّح (قرار أحمد،
  Rule 13: علّم ماتحركش). السؤالان المفتوحان: هل بتحصل أصلاً؟ ولو حصلت →
  `RTO` ولا دلو تالت ولا وسم `PARTIAL_CANCEL`؟
  ⛔ وممنوع "تصليحها" بتوسيع مقارنة `FULFILLED` وسط مهمة تانية — تحريك الدلو
  بيغيّر أرقام تاريخية في كل أداة في الستاك. → `ecommoda-order-lifecycle`
  `known-gaps.md` G-14 · التحذير مكتوب فوق الدالة في `index.js`.
- **Build watch paths لسه `*`** — أي تعديل HTML بينشر الـ Worker تاني بلا داعي.
  التضييق لـ `index.js` + `wrangler.toml` (§13-ب في `ecommoda-tool-migration-playbook`)
  مستني تأكيد إن الأداة شغالة بعد النقل. **لو اتضيّقت، أي ملف جديد يعتمد عليه الـ
  Worker لازم يتضاف للـ paths** وإلا هيفضل على نسخة قديمة من غير أي رسالة.
- **تعليق في `wrangler.toml`** فيه نقطتين `..` بدل نقطة — تجميلي بحت، متلمسش الملف
  عشانه لوحده.

آخر تحديث: 02-09-2026

</div>
